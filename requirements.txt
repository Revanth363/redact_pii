# ── Core document handling ──────────────────────────────────────────────────
python-docx>=1.1.2

# ── Faker for pseudonymization ──────────────────────────────────────────────
faker>=26.0.0

# ── Ettin NER model (Nemotron-PII via HuggingFace Transformers) ─────────────
transformers>=4.40.0
torch>=2.2.0

# ── GLiNER2 (SOTA unified extraction — requires [local] extra for inference) ─
gliner2[local]>=1.3.2

# ── Evaluation reporting ─────────────────────────────────────────────────────
tabulate>=0.9.0

# ── Optional: faster tokenisation used by transformers ──────────────────────
tokenizers>=0.19.0

